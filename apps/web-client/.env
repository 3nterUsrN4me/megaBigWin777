# WebSocket gateway URL (local dev)
VITE_WS_URL=ws://localhost:3001/ws

# Dev JWT secret — MUST match JWT_SECRET in gateway/ws
# Change this if you set a custom JWT_SECRET when starting the gateway
VITE_JWT_SECRET=dev-secret-for-testing-CHANGE-IN-PROD!!
